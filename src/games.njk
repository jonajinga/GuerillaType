---
layout: layouts/base.njk
title: "Games"
description: "Typing mini-games built on your practice data. Catch the Word turns your missed-words list into a falling-word arcade."
permalink: /games/
pageSlug: games
---
<header class="page__head">
  <p class="page__eyebrow">Play</p>
  <h1>Games</h1>
  <p class="page__subtitle">Mini-games built on your practice data. Each one targets a specific habit — speed, accuracy, the words you keep missing.</p>
</header>

<div class="game-grid">
  <article class="game-card">
    <div class="game-card__icon" aria-hidden="true">
      <svg viewBox="0 0 24 24" width="36" height="36" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
        <path d="M12 2v6"/>
        <path d="M6 8 12 14 18 8"/>
        <path d="M3 20h18"/>
      </svg>
    </div>
    <h2 class="game-card__title">Catch the Word</h2>
    <p class="game-card__desc">Words fall from the top. Type each one before it hits the bottom. The picker biases toward words you've mistyped in past sessions, so playing is also targeted practice.</p>
    <ul class="game-card__meta">
      <li><strong>Source</strong> Your missed-words list</li>
      <li><strong>Length</strong> Until 3 misses</li>
      <li><strong>Difficulty</strong> Slider, 0.5x – 2.5x</li>
    </ul>
    <div class="game-card__best" data-game-best="classic" hidden>
      <span class="game-card__best-label">Your best</span>
      <span class="game-card__best-score" data-game-best-score>0</span>
      <span class="game-card__best-streak" data-game-best-streak></span>
    </div>
    <a class="btn btn--primary" href="/practice/game/">Play Catch the Word →</a>
  </article>

  <article class="game-card" id="shooter">
    <div class="game-card__icon" aria-hidden="true">
      <svg viewBox="0 0 24 24" width="36" height="36" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
        <circle cx="12" cy="12" r="9"/>
        <circle cx="12" cy="12" r="3"/>
        <line x1="12" y1="2" x2="12" y2="6"/>
        <line x1="12" y1="18" x2="12" y2="22"/>
        <line x1="2" y1="12" x2="6" y2="12"/>
        <line x1="18" y1="12" x2="22" y2="12"/>
      </svg>
    </div>
    <h2 class="game-card__title">Word Shooter</h2>
    <p class="game-card__desc">Words drift across the screen from left to right. Type each one to shoot it before it leaves the frame. Tests target acquisition alongside typing speed.</p>
    <ul class="game-card__meta">
      <li><strong>Source</strong> Your missed-words list</li>
      <li><strong>Length</strong> Until 3 misses</li>
      <li><strong>Direction</strong> Horizontal drift</li>
    </ul>
    <div class="game-card__best" data-game-best="shooter" hidden>
      <span class="game-card__best-label">Your best</span>
      <span class="game-card__best-score" data-game-best-score>0</span>
      <span class="game-card__best-streak" data-game-best-streak></span>
    </div>
    <a class="btn btn--primary" href="/practice/game/?mode=shooter">Play Shooter →</a>
  </article>

  <article class="game-card" id="endless">
    <div class="game-card__icon" aria-hidden="true">
      <svg viewBox="0 0 24 24" width="36" height="36" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
        <path d="M12 4v16"/>
        <path d="m8 8 4-4 4 4"/>
        <path d="m8 16 4 4 4-4"/>
      </svg>
    </div>
    <h2 class="game-card__title">Endless</h2>
    <p class="game-card__desc">Words spawn forever, faster each catch. No three-miss cap — keep typing as long as you can stand it. Persistent high score per profile.</p>
    <ul class="game-card__meta">
      <li><strong>Source</strong> Your missed-words list</li>
      <li><strong>Length</strong> Until you stop</li>
      <li><strong>Difficulty</strong> Ramps faster than Classic</li>
    </ul>
    <div class="game-card__best" data-game-best="endless" hidden>
      <span class="game-card__best-label">Your best</span>
      <span class="game-card__best-score" data-game-best-score>0</span>
      <span class="game-card__best-streak" data-game-best-streak></span>
    </div>
    <a class="btn btn--primary" href="/practice/game/?mode=endless">Play Endless →</a>
  </article>
</div>

<script type="module">
/* Paint per-mode high scores from the active profile. Falls back
   silently to the hidden state if the profile / gameStats / byMode
   key isn't there yet (new user, never played). */
import { getActive } from "/assets/js/profiles.js?v={{ cssVersion }}";
(function(){
  const p = getActive();
  const gs = (p && p.gameStats) || {};
  const byMode = gs.byMode || {};
  // Legacy migration: pre-byMode profiles only had a flat
  // gameStats.highScore -- which we treat as classic-mode.
  const legacyClassic = { highScore: gs.highScore || 0, bestStreak: gs.bestStreak || 0 };
  document.querySelectorAll(".game-card__best").forEach((el) => {
    const mode = el.dataset.gameBest;
    const m = byMode[mode] || (mode === "classic" ? legacyClassic : null);
    if (!m || !m.highScore) return;
    el.hidden = false;
    const score = el.querySelector("[data-game-best-score]");
    const streak = el.querySelector("[data-game-best-streak]");
    if (score) score.textContent = m.highScore.toLocaleString();
    if (streak && m.bestStreak) streak.textContent = "best streak " + m.bestStreak;
  });
})();
</script>

<section class="games-footer" id="why">
  <h2>Why games?</h2>
  <p>The practice surface is great for deliberate work — settle in, type a quote, watch your accuracy climb. Games solve a different problem: <strong>sustained engagement</strong>. They keep you typing when you're tired or bored, and they bend the practice toward weaknesses you'd otherwise avoid (the falling-word picker is weighted by your miss count).</p>
  <p>If you have an idea for a game you'd play, <a href="/contribute/game/">pitch it via the suggestion form</a>. One round, why it's fun, what habit it builds — that's all I need.</p>
</section>
