node_modules/
_site/
.cache/
.DS_Store
*.log
.env
.env.local
.norvig-1m.txt

# `npm run audit` writes 65 PNGs here. Untracked output from the project's
# own gate should never be a trap that ends up in someone's commit.
audit/
